{
  "name": "nodejs-ghactions",
  "version": "1.0.0",
  "description": "Pure Github Actions CI: lint + test + report deploy",
  "main": "src/sum.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "ci": "npm run lint && npm run test",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0"
  }
}